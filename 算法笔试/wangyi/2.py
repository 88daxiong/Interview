#coding=utf-8
# 本题为考试多行输入输出规范示例，无需提交，不计分。
import sys

if __name__ == "__main__":
    # 读取第一行的n
    # t = int(sys.stdin.readline().strip())
    t = 1
    for i in range(t):
        result.sort()
        if nums[-1] < nums[-2] + nums[-3]:
            print("YES")
        else:
            print("NO")

# 核心点在于先将result排序